import ReactLoading from "react-loading";
function Loading(){
    return(
        <ReactLoading type="spin" color="#ff9000" height={50} width={50} className="loading" />
    );
}

export default Loading;